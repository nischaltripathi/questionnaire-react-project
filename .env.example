# Environment variables for Business Systems Assessment app
# Copy this file to .env.local (git-ignored) and fill in the values.

# Optional: HTTP endpoint to receive assessment submissions (JSON POST)
# Example: https://api.yourdomain.com/assessments
VITE_ASSESSMENTS_API_URL=

# Optional: HTTP endpoint to receive conversion events (e.g., consultation booked)
# Example: https://api.yourdomain.com/conversions
VITE_CONVERSIONS_API_URL=
